const Post = require('#models/post.model');
const PostLog = require('#models/postLog.model');
const Review = require('#models/review.model');
const User = require('#models/user.model');
module.exports = {
    Post,
    PostLog,
    Review,
    User
};